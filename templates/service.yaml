apiVersion: v1
kind: Service
metadata:
  name: {{ include "ChartPingPruebaShort.fullname" . }}
  labels:
    {{- include "ChartPingPruebaShort.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ChartPingPruebaShort.selectorLabels" . | nindent 4 }}
